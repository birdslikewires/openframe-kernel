--- a/drivers/i2c/busses/i2c-isch.c
+++ b/drivers/i2c/busses/i2c-isch.c
@@ -42,6 +42,8 @@
 #define SCH_WORD_DATA		0x03
 #define SCH_BLOCK_DATA		0x05

+#define SMBIOSIZE	8  // OpenFrame Workaround
+
 struct sch_i2c {
 	struct i2c_adapter adapter;
 	void __iomem *smba;
@@ -288,7 +290,7 @@ static int smbus_sch_probe(struct platfo
 	if (!res)
 		return -EBUSY;

-	priv->smba = devm_ioport_map(dev, res->start, resource_size(res));
+	priv->smba = devm_ioport_map(dev, res->start, SMBIOSIZE);
 	if (!priv->smba)
 		return dev_err_probe(dev, -EBUSY, "SMBus region %pR already in use!\n", res);

